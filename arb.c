#include <stdio.h>
#include <stdlib.h>
#include <funcoes.h>
#include <time.h>

int main()
{
    rn T = CriarArvore();
    int nA = 100;
    int nR = 10;
    int aux = 0;
    int *chaves = (int *) malloc(sizeof(int) * (nA));
    free(chaves);
    return 0;
}
